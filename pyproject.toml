[project]
name = "cwip"
version = "0.2.0"
authors = [
    {name = "pushfoo"}
]
maintainers = [
    {name = "pushfoo"}
]

description = "Slop-free clipboard access in pure Python."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = true

[dependency-groups]
dev =  ['pytest~=9.0.3']

[project.scripts]
cwip = "cwip:main"
